# ขั้นตอนการอัปเดตโค้ด (Capacitor + React) ไปยัง Android

เอกสารนี้รวบรวมขั้นตอนมาตรฐานสำหรับการอัปเดตโค้ดในโปรเจกต์นี้ เพื่อนำไปแสดงผลบน Android ตามลำดับขั้นตอนดังนี้ครับ

---

## 1. กรณีแก้โค้ด UI / Logic (ไฟล์ใน `src/`)
หากมีการแก้ไขไฟล์ `.tsx`, `.ts` หรือ `.css` ทั่วไป:

### ขั้นตอนการรันคำสั่ง:
```bash
# 1. Build ไฟล์เว็บใหม่
npm run build

# 2. Copy ไฟล์ที่ build แล้วเข้าไปในโปรเจกต์ Android
npx cap sync android
```

> [!TIP]
> หลังจากรันเสร็จสิ้น สามารถกดปุ่ม **Run** (ปุ่มสีเขียว) ใน **Android Studio** เพื่อทดสอบแอปได้ทันทีครับ

---

## 2. กรณีแก้ไขไฟล์ Assets (ไอคอน, รูป Splash Screen)
หากมีการเปลี่ยนรูปภาพในโฟลเดอร์ `assets/`:

### ขั้นตอนการรันคำสั่ง:
```bash
# 1. Generate ไฟล์ไอคอนใหม่ (เฉพาะตอนเปลี่ยนรูปไอคอน/Splash)
npx @capacitor/assets generate --android --assetPath assets/android

# 2. Sync ข้อมูลไปยัง Android
npx cap sync android
```

> [!IMPORTANT]
> หลังจากเสร็จสิ้นขั้นตอนนี้ ควรทำการ **Clean & Rebuild Project** ใน **Android Studio** ด้วยครับ

---

## 3. กรณีติดตั้ง Library ใหม่ (`npm install`)
หากมีการติดตั้งแพ็กเกจหรือไลบรารีใหม่เพิ่มเติม:

### ขั้นตอนการรันคำสั่ง:
```bash
# Sync เพื่อให้ Capacitor อัปเดตไฟล์ Plugin ต่างๆ ใน Android
npx cap sync android
```

---

## 4. ขั้นตอนการสร้างไฟล์ APK (`how-much-is-left.apk`) ใหม่
<<<<<<< HEAD
หากต้องการสร้างไฟล์ติดตั้ง `.apk` ตัวใหม่มาวางไว้ที่โฟลเดอร์หลักของโปรเจกต์ (Root) สามารถรันคำสั่งตามลำดับนี้ใน Terminal ได้เลยครับ:
=======
เพื่อลดความยุ่งยาก ผมได้ทำสคริปต์อัตโนมัติไว้ให้เรียบร้อยแล้วครับ พี่ไม่ต้องรันหลายคำสั่งหรือเข้าไปหาไฟล์ APK ในเครื่องเองอีกต่อไป

เพียงแค่รันคำสั่งสั้นๆ นี้ใน Terminal:
>>>>>>> f87f955549501ffaace6bbe604531ac2226ad597

### ขั้นตอนการรันคำสั่ง (สำหรับ Windows):
```bash
# 1. Build โค้ดเว็บล่าสุด
npm run build

# 2. Sync โค้ดไปยังโปรเจกต์ Android
npx cap sync android

# 3. รัน Gradle เพื่อ Compile ไฟล์ APK (เรียกผ่าน cmd เพื่อป้องกันปัญหาสายคำสั่งใน PowerShell)
cd android
cmd /c "gradlew.bat assembleDebug"
cd ..

# 4. คัดลอกไฟล์ APK ออกมาวางที่โฟลเดอร์หลักนอกสุด
cmd /c "copy android\app\build\outputs\apk\debug\app-debug.apk how-much-is-left.apk /y"
```

---

## 💡 สรุปคำสั่งที่ใช้บ่อย (Cheat Sheet)

### 1. อัปเดตโค้ดเพื่อเปิดทดสอบใน Android Studio / Emulator:
```bash
npm run build:apk
```

### สิ่งที่คำสั่งนี้จะจัดการให้โดยอัตโนมัติ:
1. ทำการ **Build เว็บ** (`vite build`)
2. ทำการ **Sync โค้ดล่าสุด** ไปยังฝั่ง Android
3. ทำการ **Compile ไฟล์ APK** ด้วย Gradle
4. ทำการ **Copy ไฟล์ `.apk` ตัวใหม่** มาวางไว้ที่โฟลเดอร์นอกสุดของโปรเจกต์ (ทับไฟล์ `how-much-is-left.apk` เดิมทันที)

---

## 💡 สรุปคำสั่งที่ใช้บ่อย (Cheat Sheet)

### 1. อัปเดตโค้ดเพื่อเปิดใน Android Studio / Emulator:
```bash
npm run build; npx cap sync android
```
<<<<<<< HEAD
*(จากนั้นกดปุ่ม **Run** สีเขียวใน Android Studio เพื่อทดสอบ)*

### 2. คำสั่งรวดเดียวจบในการสร้างไฟล์ APK ใหม่มาวางที่หน้าแรก (สำหรับ Windows - เสถียรที่สุด):
```bash
npm run build; npx cap sync android; cd android; cmd /c "gradlew.bat assembleDebug"; cd ..; cmd /c "copy android\app\build\outputs\apk\debug\app-debug.apk how-much-is-left.apk /y"
=======
*(จากนั้นสามารถกดปุ่ม **Run** สีเขียวใน Android Studio เพื่อรันแอปใหม่ได้เลย)*

### 2. สร้างไฟล์ APK ตัวใหม่มาไว้ที่หน้าแรกของโปรเจกต์ทันที:
```bash
npm run build:apk
>>>>>>> f87f955549501ffaace6bbe604531ac2226ad597
```

---

## 📝 หมายเหตุเพิ่มเติม
* ระหว่างพัฒนาเว็บ คุณสามารถรัน `npm run dev` เพื่อดูผลการทำงานบนเบราว์เซอร์ (`localhost:3000` หรือตามพอร์ตหลักของโปรเจกต์) ได้ทันทีโดยไม่จำเป็นต้อง Build ลงมือถือจริงทุกครั้ง ซึ่งจะช่วยประหยัดเวลาและทำงานได้เร็วขึ้นอย่างมากครับ